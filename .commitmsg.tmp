feat(web): 前端 UI 全面重构与现代化

- 设置/离线/登录表单统一为 fieldset+legend 语义化分组，form-grid 双列响应式布局
- 配置项文本精简统一，移除冗余说明
- 仪表盘日志区自适应高度：桌面端填满剩余视口、整页不滚动
- tab 切换加入 View Transition 动画，仅内容区过渡（侧栏/顶栏静止）
- 内容区改为填充剩余宽度并居中限宽显示
- 表单焦点态升级 :focus-visible，媒体查询改用范围语法，标题 text-wrap: balance
- 移除死代码：esc()、TORRENT_SIZE_THRESHOLD、冗余 CSS 规则
- makeView stop 参数改为可选，settings 不再传空函数
